# Nostr Relay Configuration
# Default relay for all operations
NEXT_PUBLIC_DEFAULT_RELAY=wss://yabu.me

# Search relay (NIP-50 compatible)
NEXT_PUBLIC_SEARCH_RELAY=wss://search.nos.today

# Fallback relays (comma-separated)
NEXT_PUBLIC_FALLBACK_RELAYS=wss://relay-jp.nostr.wirednet.jp,wss://r.kojira.io,wss://relay.damus.io
