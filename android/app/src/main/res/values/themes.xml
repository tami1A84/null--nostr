<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme required by AndroidManifest -->
    <!-- AppCompat base avoids dependency on platform Theme.Material which
         can be missing in stripped SDK environments. Compose handles all UI. -->
    <style name="Theme.NuruNuru" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
